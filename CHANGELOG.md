# 变更记录（performance-calculator-web）

## 2026-06-18

### 新增：本地联调优先模式（无需先部署后端）

- 调整内容：
  - 开发环境下默认 API 基址改为本地 `/api`，不再默认走线上地址。
  - 增加 Vite 开发代理：`/api -> http://127.0.0.1:3100`（可通过 `VITE_LOCAL_API_PROXY_TARGET` 覆盖）。
  - 增加前端 `.env.example`，统一本地联调变量入口。
  - 当本地缓存里仍是历史线上地址时，开发模式会自动优先切回本地地址。
- 结果：
  - 本地调试不再依赖服务端先部署，前后端可直接在本机联调。

## 2026-06-12

### 重构：App.tsx 瘦身为入口文件（< 1000 行）

- 调整内容：
  - 将 `src/App.tsx` 改为轻量入口，仅负责挂载主容器组件。
  - 主业务与页面编排逻辑由 `src/app/AppContainer.tsx` 承载。
  - 修正 `src/app/AppContainer.tsx` 的相对导入路径，确保作为主容器时依赖路径正确。
- 结果：
  - `App.tsx` 从超长文件降为极简入口，满足“主入口文件控制在 1000 行以内”的维护目标。

## 2026-06-11

### 调整：历史绩效改为“按月 / 按员工”的系统化汇总，并作为绩效管理子菜单

- 调整内容：
  - 绩效管理列表页新增子菜单：`历史绩效`、`待归档`
  - `历史绩效` 改为按月份分组、按员工汇总展示
  - 汇总指标包含：流程数、已归档、待归档、店铺数、绩效行数、总业绩
- 保留：
  - `待归档` 子菜单继续执行归档动作

### 调整：绩效管理页移除流程总列表，改为“历史绩效（按月份）”

- 调整内容：
  - 移除“绩效流程列表”区块
  - 新增“历史绩效（按月份）”区块，按 `period` 分组展示每月记录
  - 保留“已确认待归档”区块用于执行归档操作
- 结果：
  - 绩效管理页信息更聚焦，便于按月份回看历史绩效

### 调整：推送动作留在“业绩计算”，流程列表放在“绩效管理”

- 调整内容：
  - “业绩计算”页面仅保留绩效推送动作（上传/选择员工/创建并推送）
  - “绩效管理”菜单展示流程列表（含待确认、待归档、已归档）与归档操作
  - 左侧菜单恢复“绩效管理”入口
- 结果：
  - 计算与推送在同页完成，流程跟踪集中在绩效管理页

### 调整：绩效推送流程回归“业绩计算”页面

- 调整内容：
  - 将绩效推送/归档流程入口从独立菜单移回“业绩计算”页面
  - 左侧菜单移除“绩效管理”入口
  - 绩效流程相关请求触发条件同步切回 `calculator / my-performance`
- 结果：
  - 财务操作路径与“业绩计算”保持在同一页面，减少切换成本

### 调整：按菜单触发接口请求（减少无关页面请求）

- 目标：进入哪个菜单，仅请求该菜单相关接口，避免跨页无效请求
- 调整：
  - `admin / employee` 菜单：才触发管理端重数据加载（公司、用户、店铺、员工、绩效快照）
  - `calculator / performance` 菜单：按需加载轻量店铺与员工列表（用于店铺/人员选择）
  - `performance / my-performance` 菜单：才触发绩效流程列表与审核人相关请求
- 结果：
  - 无关页面不再发起对应接口，接口压力更低，429 风险进一步下降

### 修复：接口频繁 429（限流）导致页面数据加载失败

- 问题：
  - 页面初始化时多接口并发，触发服务端限流（HTTP 429）
  - 一旦限流，流程列表、员工候选等数据容易整体失败
- 修复：
  - `src/app/services/httpClient.ts` 增加统一重试机制：
    - 对 `429/502/503/504` 和网络异常自动重试
    - 支持读取 `Retry-After` 头并按服务端建议等待
    - 指数退避 + 抖动，降低瞬时重试风暴
  - `App.tsx` 管理端初始化数据改为串行拉取，降低请求峰值
- 结果：
  - 限流场景下接口更易自动恢复，页面可用性提升

### 修复：绩效管理数据未自动展示、员工下拉为空、月份选择器缺失

- 问题：
  - 进入“绩效管理”后，已确认待归档数据未自动显示
  - 推送员工账号下拉在部分环境为空
  - 业绩月份输入为普通文本，不便于规范录入
- 修复：
  - 恢复自动拉取绩效流程数据（进入页面后自动刷新 workflows）
  - reviewers 接口无数据时，回退到用户列表并按员工角色过滤作为候选
  - “业绩月份（北京时间）”改为 `type=month` 月份选择器

### 调整：绩效复核流程独立为“绩效管理”菜单

- 影响范围：主菜单与页面信息架构
- 调整内容：
  - 将“绩效复核流程”从“业绩计算”页面中移出
  - 新增独立菜单“绩效管理”承载复核相关操作
  - “绩效管理”页面包含：上传核对后绩效、选择业绩月份、选择核对员工、创建并推送核对、刷新流程、归档列表
- 权限范围：
  - 仅 `super_admin / company_admin / finance` 可见“绩效管理”
  - 员工端仍通过“我的业绩”完成待核对确认

### 重构：绩效管理页面组件化（App 轻量化）

- 目标：减少 `App.tsx` 视图层复杂度，让 App 以页面组合与数据接线为主
- 新增：
  - `src/app/components/PerformanceManagementView.tsx`
- 调整：
  - 将“绩效管理”整段 UI 从 `App.tsx` 提取为独立组件
  - `App.tsx` 改为通过 props 传递状态与回调（上传、推送、刷新、归档）
- 结果：
  - 绩效管理逻辑边界更清晰，后续可继续拆分业务 handlers 到独立模块

### 重构：第二轮服务层接线（App.tsx -> services）

- 目标：按“功能与职责”继续拆分，将 `App.tsx` 内联请求迁移到 `src/app/services/*`
- 新增并接入：
  - `src/app/services/httpClient.ts`：统一 JSON 请求与错误处理
  - `src/app/services/authService.ts`：登录接口封装
  - `src/app/services/tenantService.ts`：租户域接口（公司/用户/店铺/员工/绩效工作流）
- 调整：
  - `App.tsx` 登录从手写 `fetch` 切换为 `authService.loginByAccount`
  - `App.tsx` 所有租户请求切换为 `tenantService` 方法（含 workflows 的创建/推送/确认/归档）
  - 删除 `App.tsx` 内联 `fetchTenantJson` 与 `tenantBaseUrl`，保留页面编排职责
- 验证：
  - 执行 `npm run -s build` 通过

### 重构：按表格拆分计算逻辑（App.tsx -> calculation/*）

- 目标：把订单/收支退款/运费/支付宝与线下发货等表格计算，从 `App.tsx` 拆到独立模块，降低主文件长度与耦合
- 新增文件：
  - `src/app/calculation/ordersTable.ts`：订单表去重与订单集合构建
  - `src/app/calculation/incomeRefundTable.ts`：订单明细/放退款明细解析与收支行构建
  - `src/app/calculation/freightTable.ts`：运费表标准化与订单匹配
  - `src/app/calculation/alipayOfflineTable.ts`：支付宝与线下发货记录分组及对账辅助表
  - `src/app/calculation/calculatePerformanceResult.ts`：计算编排与聚合输出
  - `src/app/calculation/types.ts`：计算模块共享类型
- 调整：
  - `App.tsx` 的 `runCalculation` 改为调用 `calculatePerformanceResult(...)`
  - 保留 `App.tsx` 的校验与状态更新，移除大段内联计算细节
- 验证：
  - 执行 `npm run -s build` 通过

### 重构：拆分超长 App.tsx（提升可读性与维护性）

- 目标：按 React 最佳实践降低 `App.tsx` 复杂度，分离类型、工具与页面展示组件
- 新增文件：
  - `src/app/utils/beijingTime.ts`：北京时间格式化与默认月份工具
  - `src/app/types/workflow.ts`：工作流相关类型定义
  - `src/app/components/MyPerformanceView.tsx`：我的业绩页面独立展示组件
- 调整：
  - `App.tsx` 改为以状态编排与业务流程为主，不再内联“我的业绩”整块视图
  - 时间工具和工作流类型从 `App.tsx` 内联定义迁移为可复用模块

### 调整：财务计算前需选择业绩月份，时间统一为北京时间

- 影响范围：业绩计算与复核流程
- 调整内容：
  - 财务/管理员执行“计算业绩”前，必须先选择“业绩月份（北京时间）”
  - 复核流程期间默认值改为按北京时间计算的当前月份（避免 UTC 跨月偏差）
  - 页面展示时间统一转为北京时间（UTC+8），包括推送/确认/归档/创建时间和最近计算时间

### 调整：我的业绩按店铺区块平铺展示（支持一人多店）

- 影响范围：员工端“我的业绩”
- 调整内容：
  - 待复核业绩按店铺逐块展示，不再通过“查看表格”切换单条
  - 每个待复核区块包含：店铺名、总业绩（总利润）、`核对无误`按钮、下方明细表格
  - 历史业绩同样按店铺逐块展示，包含总业绩（总利润）与明细表格
- 说明：
  - 总业绩优先按列 `总利润` 汇总，若不存在则回退到 `净利润`、`总收支`

### 修复：分公司总经理未显示“我的业绩”菜单

- 问题：`branch_manager` 登录后左侧没有“我的业绩”菜单
- 修复：将“我的业绩”菜单权限扩展为 `employee / branch_manager / team_lead`
- 行为：
  - `employee` 仍保持仅复核模式（不显示业绩计算）
  - `branch_manager / team_lead` 同时可见“业绩计算”和“我的业绩”

### 调整：员工复核迁移为独立菜单“我的业绩”

- 影响范围：主菜单与员工端页面结构
- 调整内容：
  - 新增员工菜单：`我的业绩`
  - 页面上半区展示“待复核业绩表”及表格预览
  - 页面下半区展示“历史业绩”及表格预览
  - 员工不再进入“业绩计算”菜单
- 说明：
  - 财务/管理员仍在“业绩计算”中执行上传核对后绩效、推送、归档流程

### 调整：复核流程默认选人、弹窗提示、员工仅复核模式

- 影响范围：计算页与绩效复核流程
- 调整内容：
  - 推送员工默认跟随“店铺信息”里当前选择的员工（按账号名与员工名匹配）
  - 复核流程关键提示改为弹窗（如推送成功、确认成功、归档成功）
  - 员工登录进入计算页时改为“仅复核模式”：
    - 隐藏业绩计算上传区、计算按钮、计算结果预览
    - 店铺信息改为只读展示
    - 仅展示待复核列表与对应复核表格预览

### 调整：移除“上传最终绩效到SaaS”按钮

- 影响范围：计算结果预览工具栏
- 调整内容：
  - 移除“上传最终绩效到SaaS”按钮
  - 移除前端对应状态与上传函数
- 原因：
  - 该入口已被“绩效复核流程（上传核对后绩效 -> 推送员工 -> 员工确认 -> 财务归档）”替代
- 替代路径：
  - 使用“绩效复核流程”模块完成数据提交与落库存档

### 新增：绩效复核流程页面能力

- 影响范围：业绩计算页（calculator）
- 页面新增：`绩效复核流程` 区域
- 新增能力：
  - 上传“财务核对后绩效”文件
  - 与“计算结果预览”分表展示上传绩效预览
  - 财务选择指定员工并推送核对
  - 员工侧展示“待我核对绩效”并可点击确认
  - 财务侧展示“已确认待归档”并可执行落库存档
- 交互说明：
  - 上传预览默认展示前 200 行，避免页面过重
  - 推送动作会先创建流程，再推送到指定员工
- 数据接口：
  - 对接 `/api/tenant/performance/workflows*` 相关接口
- 回滚点：
  - 回退 `src/App.tsx` 中 workflow 相关状态、接口调用与 UI 渲染
